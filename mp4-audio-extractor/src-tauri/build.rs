// ===================================================================
// build.rs — Tauri 构建脚本
// ===================================================================
//
// 本文件在 cargo build 时自动执行（编译前阶段）。
// tauri_build::build() 负责：
//   1. 读取 tauri.conf.json 配置
//   2. 生成 Windows 资源文件（图标、版本信息）
//   3. 设置编译时环境变量（如 TAURI_ENV_TARGET_TRIPLE）
//   4. 检查文件变更以触发重新编译
fn main() {
    tauri_build::build()
}
